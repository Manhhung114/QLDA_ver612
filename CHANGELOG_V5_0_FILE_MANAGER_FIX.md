# V5.0 - File Manager UI Fix

- Khung `☁ Quản lý file Google Drive` luôn hiện trong tất cả sheet Hồ sơ/Bản vẽ khi đã có bản ghi.
- Không còn phụ thuộc vào selectbox sửa hồ sơ/bản vẽ; tránh mất nút Upload sau `st.rerun()`.
- Có selector riêng `Chọn bản ghi để tải lên / tải xuống file`.
- Nút `⬆️ TẢI FILE LÊN GOOGLE DRIVE (2GB)` hiện cho Cập nhật/Admin.
- Mỗi file Drive có `☁ Mở` và `⬇️ Tải xuống`.
- Xóa file/bản ghi vẫn chỉ Admin.
- Sau khi lưu mới, app ghi nhớ bản ghi vừa lưu và tự chọn trong khung quản lý file.
